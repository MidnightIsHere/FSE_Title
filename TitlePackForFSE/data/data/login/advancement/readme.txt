I can't put annotation into the json files
so i build this readme to mention you

welcome.json is a advancement to control the first welcome for every player
if you want to revoke it
use this command:
/advancement revoke @s login:welcome
